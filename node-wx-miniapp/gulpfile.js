const gulp = require('gulp');

gulp.task('default', function () {
    // to do 
    // return gulp.src([
    //     // 'log',
    //     // 'resources/**/*'
    // ], { base: '.' }).pipe(gulp.dest('dist'));
});